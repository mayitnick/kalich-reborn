version: "3.9"
services:
  # [АРХИВ] PWA фронтенд временно законсервирован в archive/pwa/
  # frontend:
  #   build:
  #     context: .
  #     dockerfile: archive/Dockerfile.frontend
  #   container_name: kalich_frontend
  #   restart: always
  #   ports:
  #     - "80:80"
  #   environment:
  #     - NODE_ENV=production
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: kalich_backend
    restart: always
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ./data:/app/data
